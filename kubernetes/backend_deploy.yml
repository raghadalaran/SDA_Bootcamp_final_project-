apiVersion: apps/v1
kind: Deployment
metadata:
 name: backend-deploy
 labels:
   app: backend-app
 namespace: techmomentum-project
spec:
 replicas: 2
 selector:
   matchLabels:
     app: backend-app
 template:
   metadata:
     name: backend-app
     labels:
       app: backend-app
   spec:
     containers:
       - name: backend-app
         image: samahmoe/api-auth-app:latest
         ports:
           - containerPort: 3000
         env:
           - name: DB_SERVER
             value: "devops1-week12-sql.database.windows.net"
           - name: DB_USER
             value: "techmomentum"
           - name: DB_PASSWORD
             value: "password123456@"
           - name: DB_NAME
             value: "devops1-week10-sqldb"
